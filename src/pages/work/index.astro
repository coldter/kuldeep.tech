---
import { getCollection } from "astro:content";

import BaseLayout from "../../layouts/BaseLayout.astro";

import ContactCTA from "../../components/ContactCTA.astro";
import PortfolioPreview from "../../components/PortfolioPreview.astro";
import Hero from "../../components/Hero.astro";
import Grid from "../../components/Grid.astro";

const projects = (await getCollection("work")).sort(
  (a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf(),
);
---

<BaseLayout
  title="Work/Writing | Kuldeep Parmar"
  description="Past projects, client work, and writing"
>
  <div class="stack gap-20">
    <main class="wrapper stack gap-8">
      <Hero
        title="Work/Writing"
        tagline="Past projects and client work, plus the occasional writing and side-project I get nerdy about."
        align="start"
      />
      <Grid variant="offset">
        {
          projects.map((project, index) => (
            <li>
              <PortfolioPreview
                project={project}
                loading={[0, 1, 3].includes(index) ? "eager" : "lazy"}
              />
            </li>
          ))
        }
      </Grid>
    </main>
    <ContactCTA />
  </div>
</BaseLayout>
