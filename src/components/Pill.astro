<div class="pill"><slot /></div>

<style>
  /* An engraved legend chip: recessed into the panel, printed in mono caps.
     Square-ish on purpose — a pill would read as a generic badge. */
  .pill {
    display: inline-flex;
    align-items: center;
    padding: 0.4rem 0.7rem 0.4rem 0.6rem;
    gap: 0.45rem;
    color: var(--gray-200);
    border: 1px solid var(--surface-raised-border);
    background-color: var(--surface-raised);
    border-radius: 0.375rem;
    box-shadow: var(--bevel-raised), var(--shadow-sm);
    font-size: var(--text-sm);
    font-family: var(--font-mono);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    line-height: 1.35;
    white-space: nowrap;
    max-width: 100%;
    overflow: hidden;
    text-overflow: ellipsis;
    transition:
      border-color 200ms ease-out,
      color 200ms ease-out,
      background-color 200ms ease-out;
  }

  /* Tick gauge on the leading edge — the same mark as the plate's scale. */
  .pill::before {
    content: "";
    flex: none;
    width: 0.4rem;
    height: 0.7rem;
    background-image: repeating-linear-gradient(
      180deg,
      var(--tick) 0 1px,
      transparent 1px 3px
    );
  }

  .pill :global(svg) {
    color: var(--color-accent-text);
  }

  .pill:hover {
    border-color: var(--gray-600);
    color: var(--gray-0);
  }
</style>
